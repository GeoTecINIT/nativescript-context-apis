import { AccelerometerRecords } from "./accelerometer-recorder";

export function extractFeatureFromRecords(records: AccelerometerRecords) {
    // TODO: DO SOMETHING (⌐■_■) Well, I already have the code 😊
}